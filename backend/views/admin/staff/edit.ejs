<%
const titleCase = (value) => String(value || '-').replace(/_/g, ' ').replace(/\b\w/g, (char) => char.toUpperCase());
%>

<%- include('../partials/shell-start', {
  heading: 'Edit Staff',
  description: 'Perbarui profil staff. Password hanya berubah jika diisi.'
}) %>

<section class="crud-panel form-panel user-form-panel">
  <a href="/admin/staff" class="back-link"><i class="ri-arrow-left-line"></i>Back to Staff</a>
  <form method="POST" action="/admin/staff/<%= staff.id %>/update" class="crud-form user-form" novalidate>
    <div class="form-grid">
      <label>
        <span>Name</span>
        <input type="text" name="name" value="<%= staff.name || '' %>" required />
        <% if (errors.name) { %><small class="field-error"><%= errors.name %></small><% } %>
      </label>
      <label>
        <span>Email</span>
        <input type="email" name="email" value="<%= staff.email || '' %>" required />
        <% if (errors.email) { %><small class="field-error"><%= errors.email %></small><% } %>
      </label>
      <label>
        <span>Phone</span>
        <input type="text" name="phone" value="<%= staff.phone || '' %>" />
      </label>
      <label>
        <span>Status</span>
        <select name="status">
          <% allowedStatuses.forEach((status) => { %>
            <option value="<%= status %>" <%= staff.status === status ? 'selected' : '' %>><%= titleCase(status) %></option>
          <% }); %>
        </select>
        <% if (errors.status) { %><small class="field-error"><%= errors.status %></small><% } %>
      </label>
      <label>
        <span>New Password</span>
        <input type="password" name="password" minlength="8" placeholder="Leave blank to keep current password" />
        <% if (errors.password) { %><small class="field-error"><%= errors.password %></small><% } %>
      </label>
      <label>
        <span>Role</span>
        <input type="text" value="staff" disabled />
      </label>
    </div>
    <div class="form-actions">
      <a href="/admin/staff" class="crud-btn ghost">Cancel</a>
      <button type="submit" class="crud-btn primary">Save Changes</button>
    </div>
  </form>
</section>

<%- include('../partials/shell-end') %>
